/**
 * miniz_export.h（本ファイルは本来CMakeのビルド時に自動生成されるものであり、
 * リポジトリには含まれていない。本プロジェクトはminizをスタティックに
 * 直接ビルドへ組み込むだけでDLL化はしないため、MINIZ_EXPORTは空でよい）
 */
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H

#define MINIZ_EXPORT

#endif /* MINIZ_EXPORT_H */
